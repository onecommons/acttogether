skeleton original built using `express --sessions --css less --ejs ocdemo`
to install run `npm install` after cloning repo (use `npm ls` to verify dependencies)
install mongodb: e.g. sudo brew install mongodb; sudo mkdir -p /data/db; sudo chown `id -u` /data/db
to run app, in two consoles run:
  sudo mongod 
  DEBUG=express:* node app.js
run unit tests:
  NODE_ENV=test ./node_modules/.bin/mocha --reporter=nyan
